"use client";

export default function TemplateViewerPage() {
  return (
    <div className="flex-1 flex items-center justify-center" style={{ color: "var(--text-muted)" }}>
      Viewer template — va fi implementat in Faza 3
    </div>
  );
}
