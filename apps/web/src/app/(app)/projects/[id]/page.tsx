"use client";

export default function ProjectViewPage() {
  return (
    <div className="flex-1 flex items-center justify-center" style={{ color: "var(--text-muted)" }}>
      Detalii proiect — va fi implementat in Faza 4
    </div>
  );
}
