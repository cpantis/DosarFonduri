# Railway config — monorepo
# Build config is set per-service via RAILWAY_DOCKERFILE_PATH env var
# DO NOT add [build] section here — it would override all services

[deploy]
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
